#include "parser.h"


<N> => class <id> { <class_body> } <class_def>
int LLclass(){
    
    Token * token;
    token=getToken()
    if(token->type == )
}

void parseStart(){
    int error = 0;
    
    if(LLparserID(), LL
    
    Token *token;
    
    
    
    
}